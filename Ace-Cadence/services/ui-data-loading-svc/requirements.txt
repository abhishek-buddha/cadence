# All dependencies come from the ace-cadence-base image (see base-image/requirements.txt).
# httpx (service-to-service calls) and redis (pub/sub) are both already in the
# base image's requirements.txt.
