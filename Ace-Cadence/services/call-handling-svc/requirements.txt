# All dependencies come from the ace-cadence-base image (see base-image/requirements.txt).
# This file exists so the Dockerfile's `pip install -r requirements.txt` step
# has something to run; add service-specific extras here if this service ever
# needs a package the base image doesn't already provide.
